# Scenario GTFS no-shapes zips

The former per-scenario no-shapes GTFS zip files were replaced by the content-addressed component store in `../scenario_gtfs_components/`.

Use `code/reconstruct_full_gtfs_with_shapes.py` from the package root to rebuild no-shapes or full GTFS zip files.
