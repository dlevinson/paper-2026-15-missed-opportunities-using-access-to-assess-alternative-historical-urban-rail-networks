# Scenario GTFS component store

Generated: 2026-05-23 10:59:33 AEST

This directory stores `59` unique GTFS member components used by `29` scenario feeds. The former per-scenario `data/scenario_gtfs_no_shapes/*.zip` files were removed because many GTFS tables were repeated across scenarios.

Use `code/reconstruct_full_gtfs_with_shapes.py` to rebuild either the intermediate no-shapes feeds or the full feeds with `shapes.txt` restored.
